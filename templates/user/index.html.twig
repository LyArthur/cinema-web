{% extends 'base.html.twig' %}

{% block title %}Hello !{% endblock %}

{% block body %}
    {{ form_start(form) }}
        {{ form_row(form.email) }}
        {{ form_row(form.password) }}
        {{ form_row(form.confirmPassword) }}
        <button class="btn btn-primary" type="submit" style="margin-top: 20px">Créer un compte</button>
    {{ form_end(form) }}
{% endblock %}
